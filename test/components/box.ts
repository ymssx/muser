import { Element } from '../../src/index';

export default class Box extends Element {
  constructor() {
    super();
  }

  paint() {
    const ctx = this.context;

    // ctx?.moveTo(10, 10);
  }
}
